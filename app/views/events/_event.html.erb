<li>
  <%= link_to event.name, event %>
  <% if signed_in? && current_user.admin? %>
  | <%= button_to "delete", event, :method => :delete, :confirm => "You sure?",
                                :title => "Delete #{event.name}" %>
  <% end %>
</li>
